const colors = {
  textColor: "#222",
  pageBackground: "#fff",
  titleColor: "#1260c3",
  borderColor: "#ddd",
  hightlightRow: "#eaeaea",
};

export const defaultTheme = {
  ...colors,
};

export default defaultTheme;
